/// Namespace for package-level constants. `version` exists so the scaffold has
/// something real to assert against rather than a tautology.
public enum FlockCore {
    public static let version = "1"
}
